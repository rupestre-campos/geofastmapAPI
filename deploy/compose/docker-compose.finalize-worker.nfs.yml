# NFS bind mounts for finalize worker on the API host (match docker-compose.api.nfs.yml paths).
#
#   docker compose -f deploy/compose/docker-compose.finalize-worker.yml \
#     -f deploy/compose/docker-compose.finalize-worker.nfs.yml up -d --build

name: geofastmap_api

services:
  finalize_worker:
    volumes:
      - /srv/geofast/bulk-uploads:/data/bulk-uploads
