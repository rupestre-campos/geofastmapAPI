# nginx examples

- **`titiler-upstream.conf`** — Load balance several Titiler backends; set API `TITILER_INTERNAL_URL` to this listener (e.g. `http://nginx:8080` on a shared Docker network).

See [`docs/DEPLOYMENT.md`](../../docs/DEPLOYMENT.md).
